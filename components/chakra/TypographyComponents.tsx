"use client";

//We need to identify the "use client" of the component
//just import and export the component directly, then we can use it everywhere

import { Heading, Highlight, Text } from "@chakra-ui/react";

export { Heading, Highlight, Text };
